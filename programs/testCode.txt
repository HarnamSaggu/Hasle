main(args) {
    println("hello world".len())
    println(arr()[2].bar[2].bar / arr()[1].bar)
}

fun arr() {
    arr = {foo(1), foo(2), foo({3, 1, foo(4)})}
    <- arr
}

struct foo(num) {
    bar = num
}
